@model IEnumerable<MyStoreDomain.Entities.MyItem>
@{
    ViewBag.Title = "Administracja: Wszystkie produkty";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<div class="panel panel-default">
    <div class="panel-heading">
        <h3>Wszystkie produkty</h3>
    </div>
    <div class="panel-body">
        <table class="table table-striped table-condensed table-bordered">
            <tr>
                <th>ID</th>
                <th>Nazwa</th>
                <th class="NumericCol">Cena</th>
                <th>Akcje</th>
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.NumberID</td>
                    <td>@Html.ActionLink(item.Name1, "Edit", new { productId = item.NumberID })</td>
                    <td class="NumericCol">@item.Price.ToString("c")</td>
                    <td>
                        @using (Html.BeginForm("Delete", "Admin"))
                        {
                            @Html.Hidden("ProductID", item.NumberID)
                            <input type="submit" value="Usuń" />
                        }
                    </td>
                </tr>
            }
        </table>
    </div>
    <div class="panel-footer">
        @Html.ActionLink("Dodaj nowy produkt", "Create", null,
new { @class = "btn btn-default" })
    </div>
</div>
